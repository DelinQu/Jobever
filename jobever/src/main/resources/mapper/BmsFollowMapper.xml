<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.lin.mapper.BmsFollowMapper">
    <select id="getFollowList" resultType="com.lin.model.entity.UmsUser">
        select *
        from ums_user u
        LEFT JOIN bms_follow f on u.id = f.parent_id
        where f.follower_id = #{userId}
        order by u.score desc
    </select>

    <select id="getFansList" resultType="com.lin.model.entity.UmsUser">
        select *
        from ums_user u
                 LEFT JOIN bms_follow f on u.id = f.follower_id
        where f.parent_id = #{userId}
        order by u.score desc
    </select>

</mapper>
